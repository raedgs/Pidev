<form method="post" action="{{ path('type_a_v_delete', {'id': type_a_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_a_v.id) }}">
    <button class="btn">Supprimer</button>
</form>
